# 优先读取上下文文件规则

## 核心指令
你每次启动新对话、接收新任务、或者切换模式时，你的【绝对第一步行动（Absolute First Action）】必须是使用 `read_file` 工具去读取项目根目录下的 `AGENTS.md`。

## 行为约束
1. 严禁在未读取 `AGENTS.md` 之前使用任何 `list_dir`（遍历目录）或 `search_grep`（全局搜索）工具。
2. 必须先通过该文件了解项目架构，严禁盲目地将整个代码库作为上下文扫描，必须最大化节省 Token 消耗。